---
uid: web-api/overview/security/authentication-and-authorization-in-aspnet-web-api
title: Autenticação e Autorização em ASP.NET API web | Microsoft Docs
author: MikeWasson
description: Dá uma visão geral da autenticação e da autorização em ASP.NET API da Web.
ms.author: riande
ms.date: 11/27/2012
ms.assetid: 6dfb51ea-9f4d-4e70-916c-8ef8344a88d6
msc.legacyurl: /web-api/overview/security/authentication-and-authorization-in-aspnet-web-api
msc.type: authoredcontent
ms.openlocfilehash: 368d2b9456d12b2bb4063a23333e5c8837faa3b8
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/06/2020
ms.locfileid: "80676254"
---
# <a name="authentication-and-authorization-in-aspnet-web-api"></a><span data-ttu-id="bd7d7-103">Autenticação e autorização no ASP.NET Web API</span><span class="sxs-lookup"><span data-stu-id="bd7d7-103">Authentication and Authorization in ASP.NET Web API</span></span>

<span data-ttu-id="bd7d7-104">por [Mike Wasson](https://github.com/MikeWasson)</span><span class="sxs-lookup"><span data-stu-id="bd7d7-104">by [Mike Wasson](https://github.com/MikeWasson)</span></span>

<span data-ttu-id="bd7d7-105">Você criou uma API web, mas agora quer controlar o acesso a ela.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-105">You've created a web API, but now you want to control access to it.</span></span> <span data-ttu-id="bd7d7-106">Nesta série de artigos, veremos algumas opções para proteger uma API web de usuários não autorizados.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-106">In this series of articles, we'll look at some options for securing a web API from unauthorized users.</span></span> <span data-ttu-id="bd7d7-107">Esta série cobrirá tanto a autenticação quanto a autorização.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-107">This series will cover both authentication and authorization.</span></span>

- <span data-ttu-id="bd7d7-108">*Autenticação* é saber a identidade do usuário.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-108">*Authentication* is knowing the identity of the user.</span></span> <span data-ttu-id="bd7d7-109">Por exemplo, Alice faz login com seu nome de usuário e senha, e o servidor usa a senha para autenticar Alice.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-109">For example, Alice logs in with her username and password, and the server uses the password to authenticate Alice.</span></span>
- <span data-ttu-id="bd7d7-110">*A autorização* é decidir se um usuário pode realizar uma ação.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-110">*Authorization* is deciding whether a user is allowed to perform an action.</span></span> <span data-ttu-id="bd7d7-111">Por exemplo, Alice tem permissão para obter um recurso, mas não criar um recurso.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-111">For example, Alice has permission to get a resource but not create a resource.</span></span>

<span data-ttu-id="bd7d7-112">O primeiro artigo da série dá uma visão geral da autenticação e da autorização em ASP.NET API da Web.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-112">The first article in the series gives a general overview of authentication and authorization in ASP.NET Web API.</span></span> <span data-ttu-id="bd7d7-113">Outros tópicos descrevem cenários comuns de autenticação para API web.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-113">Other topics describe common authentication scenarios for Web API.</span></span>

> [!NOTE]
> <span data-ttu-id="bd7d7-114">Graças às pessoas que revisaram esta série e forneceram feedback valioso: Rick Anderson, Levi Broderick, Barry Dorrans, Tom Dykstra, Hongmei Ge, David Matson, Daniel Roth, Tim Teebken.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-114">Thanks to the people who reviewed this series and provided valuable feedback: Rick Anderson, Levi Broderick, Barry Dorrans, Tom Dykstra, Hongmei Ge, David Matson, Daniel Roth, Tim Teebken.</span></span>

## <a name="authentication"></a><span data-ttu-id="bd7d7-115">Autenticação</span><span class="sxs-lookup"><span data-stu-id="bd7d7-115">Authentication</span></span>

<span data-ttu-id="bd7d7-116">A API da Web pressupõe que a autenticação acontece no host.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-116">Web API assumes that authentication happens in the host.</span></span> <span data-ttu-id="bd7d7-117">Para hospedagem na Web, o host é o IIS, que usa módulos HTTP para autenticação.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-117">For web-hosting, the host is IIS, which uses HTTP modules for authentication.</span></span> <span data-ttu-id="bd7d7-118">Você pode configurar seu projeto para usar qualquer um dos módulos de autenticação incorporados ao IIS ou ASP.NET, ou escrever seu próprio módulo HTTP para executar a autenticação personalizada.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-118">You can configure your project to use any of the authentication modules built in to IIS or ASP.NET, or write your own HTTP module to perform custom authentication.</span></span>

<span data-ttu-id="bd7d7-119">Quando o host autentica o usuário, ele cria um *principal*, que é um objeto [IPrincipal](https://msdn.microsoft.com/library/System.Security.Principal.IPrincipal.aspx) que representa o contexto de segurança sob o qual o código está sendo executado.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-119">When the host authenticates the user, it creates a *principal*, which is an [IPrincipal](https://msdn.microsoft.com/library/System.Security.Principal.IPrincipal.aspx) object that represents the security context under which code is running.</span></span> <span data-ttu-id="bd7d7-120">O host conecta o principal ao segmento atual definindo **Thread.CurrentPrincipal**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-120">The host attaches the principal to the current thread by setting **Thread.CurrentPrincipal**.</span></span> <span data-ttu-id="bd7d7-121">O principal contém um objeto **de identidade** associado que contém informações sobre o usuário.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-121">The principal contains an associated **Identity** object that contains information about the user.</span></span> <span data-ttu-id="bd7d7-122">Se o usuário for autenticado, a propriedade **Identity.IsAuthenticated** será **verdadeira**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-122">If the user is authenticated, the **Identity.IsAuthenticated** property returns **true**.</span></span> <span data-ttu-id="bd7d7-123">Para solicitações anônimas, **o IsAuthenticated** retorna **falso**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-123">For anonymous requests, **IsAuthenticated** returns **false**.</span></span> <span data-ttu-id="bd7d7-124">Para obter mais informações sobre os diretores, consulte [Segurança baseada em papéis](https://msdn.microsoft.com/library/shz8h065.aspx).</span><span class="sxs-lookup"><span data-stu-id="bd7d7-124">For more information about principals, see [Role-Based Security](https://msdn.microsoft.com/library/shz8h065.aspx).</span></span>

### <a name="http-message-handlers-for-authentication"></a><span data-ttu-id="bd7d7-125">Manipuladores de mensagens HTTP para autenticação</span><span class="sxs-lookup"><span data-stu-id="bd7d7-125">HTTP Message Handlers for Authentication</span></span>

<span data-ttu-id="bd7d7-126">Em vez de usar o host para autenticação, você pode colocar a lógica de autenticação em um [manipulador de mensagens HTTP](../advanced/http-message-handlers.md).</span><span class="sxs-lookup"><span data-stu-id="bd7d7-126">Instead of using the host for authentication, you can put authentication logic into an [HTTP message handler](../advanced/http-message-handlers.md).</span></span> <span data-ttu-id="bd7d7-127">Nesse caso, o manipulador de mensagens examina a solicitação HTTP e define o principal.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-127">In that case, the message handler examines the HTTP request and sets the principal.</span></span>

<span data-ttu-id="bd7d7-128">Quando você deve usar manipuladores de mensagens para autenticação?</span><span class="sxs-lookup"><span data-stu-id="bd7d7-128">When should you use message handlers for authentication?</span></span> <span data-ttu-id="bd7d7-129">Aqui estão algumas trocas:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-129">Here are some tradeoffs:</span></span>

- <span data-ttu-id="bd7d7-130">Um módulo HTTP vê todas as solicitações que passam pelo ASP.NET pipeline.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-130">An HTTP module sees all requests that go through the ASP.NET pipeline.</span></span> <span data-ttu-id="bd7d7-131">Um manipulador de mensagens só vê solicitações que são roteadas para a API da Web.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-131">A message handler only sees requests that are routed to Web API.</span></span>
- <span data-ttu-id="bd7d7-132">Você pode definir manipuladores de mensagens por rota, o que permite aplicar um esquema de autenticação a uma rota específica.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-132">You can set per-route message handlers, which lets you apply an authentication scheme to a specific route.</span></span>
- <span data-ttu-id="bd7d7-133">Os módulos HTTP são específicos do IIS.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-133">HTTP modules are specific to IIS.</span></span> <span data-ttu-id="bd7d7-134">Os manipuladores de mensagens são agnósticos de host, para que possam ser usados tanto com hospedagem na Web quanto com auto-hospedagem.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-134">Message handlers are host-agnostic, so they can be used with both web-hosting and self-hosting.</span></span>
- <span data-ttu-id="bd7d7-135">Os módulos HTTP participam do registro, auditoria do IIS e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-135">HTTP modules participate in IIS logging, auditing, and so on.</span></span>
- <span data-ttu-id="bd7d7-136">Módulos HTTP executados mais cedo no pipeline.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-136">HTTP modules run earlier in the pipeline.</span></span> <span data-ttu-id="bd7d7-137">Se você lidar com a autenticação em um manipulador de mensagens, o principal não será definido até que o manipulador seja executado.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-137">If you handle authentication in a message handler, the principal does not get set until the handler runs.</span></span> <span data-ttu-id="bd7d7-138">Além disso, o principal volta para o principal anterior quando a resposta deixa o manipulador de mensagens.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-138">Moreover, the principal reverts back to the previous principal when the response leaves the message handler.</span></span>

<span data-ttu-id="bd7d7-139">Geralmente, se você não precisa suportar auto-hospedagem, um módulo HTTP é uma opção melhor.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-139">Generally, if you don't need to support self-hosting, an HTTP module is a better option.</span></span> <span data-ttu-id="bd7d7-140">Se você precisar apoiar a auto-hospedagem, considere um manipulador de mensagens.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-140">If you need to support self-hosting, consider a message handler.</span></span>

### <a name="setting-the-principal"></a><span data-ttu-id="bd7d7-141">Definindo o Principal</span><span class="sxs-lookup"><span data-stu-id="bd7d7-141">Setting the Principal</span></span>

<span data-ttu-id="bd7d7-142">Se o aplicativo executa qualquer lógica de autenticação personalizada, você deve definir o principal em dois lugares:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-142">If your application performs any custom authentication logic, you must set the principal on two places:</span></span>

- <span data-ttu-id="bd7d7-143">**Thread.CurrentPrincipal**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-143">**Thread.CurrentPrincipal**.</span></span> <span data-ttu-id="bd7d7-144">Esta propriedade é a maneira padrão de definir o principal do segmento em .NET.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-144">This property is the standard way to set the thread's principal in .NET.</span></span>
- <span data-ttu-id="bd7d7-145">**Httpcontext.current.user**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-145">**HttpContext.Current.User**.</span></span> <span data-ttu-id="bd7d7-146">Esta propriedade é específica para ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-146">This property is specific to ASP.NET.</span></span>

<span data-ttu-id="bd7d7-147">O código a seguir mostra como definir o principal:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-147">The following code shows how to set the principal:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample1.cs)]

<span data-ttu-id="bd7d7-148">Para hospedagem na Web, você deve definir o principal em ambos os lugares; caso contrário, o contexto de segurança pode se tornar inconsistente.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-148">For web-hosting, you must set the principal in both places; otherwise the security context may become inconsistent.</span></span> <span data-ttu-id="bd7d7-149">Para auto-hospedagem, no entanto, **httpContext.Current** é nulo.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-149">For self-hosting, however, **HttpContext.Current** is null.</span></span> <span data-ttu-id="bd7d7-150">Para garantir que seu código seja agnóstico de host, portanto, verifique se há nulo antes de atribuir ao **HttpContext.Current**, como mostrado.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-150">To ensure your code is host-agnostic, therefore, check for null before assigning to **HttpContext.Current**, as shown.</span></span>

## <a name="authorization"></a><span data-ttu-id="bd7d7-151">Autorização</span><span class="sxs-lookup"><span data-stu-id="bd7d7-151">Authorization</span></span>

<span data-ttu-id="bd7d7-152">A autorização acontece mais tarde no oleoduto, mais perto do controlador.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-152">Authorization happens later in the pipeline, closer to the controller.</span></span> <span data-ttu-id="bd7d7-153">Isso permite que você faça escolhas mais granulares quando você concede acesso aos recursos.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-153">That lets you make more granular choices when you grant access to resources.</span></span>

- <span data-ttu-id="bd7d7-154">*Os filtros de autorização* são executados antes da ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-154">*Authorization filters* run before the controller action.</span></span> <span data-ttu-id="bd7d7-155">Se a solicitação não for autorizada, o filtro retorna uma resposta de erro e a ação não é invocada.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-155">If the request is not authorized, the filter returns an error response, and the action is not invoked.</span></span>
- <span data-ttu-id="bd7d7-156">Dentro de uma ação de controlador, você pode obter o principal atual da propriedade **ApiController.User.**</span><span class="sxs-lookup"><span data-stu-id="bd7d7-156">Within a controller action, you can get the current principal from the **ApiController.User** property.</span></span> <span data-ttu-id="bd7d7-157">Por exemplo, você pode filtrar uma lista de recursos com base no nome do usuário, devolvendo apenas os recursos que pertencem a esse usuário.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-157">For example, you might filter a list of resources based on the user name, returning only those resources that belong to that user.</span></span>

![](authentication-and-authorization-in-aspnet-web-api/_static/image1.png)

<a id="auth3"></a>
### <a name="using-the-authorize-attribute"></a><span data-ttu-id="bd7d7-158">Usando o atributo [Autorizar]</span><span class="sxs-lookup"><span data-stu-id="bd7d7-158">Using the [Authorize] Attribute</span></span>

<span data-ttu-id="bd7d7-159">A API da Web fornece um filtro de autorização incorporado, [AuthorizeAttribute](https://msdn.microsoft.com/library/system.web.http.authorizeattribute.aspx).</span><span class="sxs-lookup"><span data-stu-id="bd7d7-159">Web API provides a built-in authorization filter, [AuthorizeAttribute](https://msdn.microsoft.com/library/system.web.http.authorizeattribute.aspx).</span></span> <span data-ttu-id="bd7d7-160">Este filtro verifica se o usuário está autenticado.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-160">This filter checks whether the user is authenticated.</span></span> <span data-ttu-id="bd7d7-161">Caso assim, ele retorna o código de status HTTP 401 (Não Autorizado), sem invocar a ação.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-161">If not, it returns HTTP status code 401 (Unauthorized), without invoking the action.</span></span>

<span data-ttu-id="bd7d7-162">Você pode aplicar o filtro globalmente, no nível do controlador ou no nível de ações individuais.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-162">You can apply the filter globally, at the controller level, or at the level of individual actions.</span></span>

<span data-ttu-id="bd7d7-163">**Globalmente**: Para restringir o acesso a cada controlador de API da Web, adicione o filtro **AuthorizeAttribute** à lista global de filtros:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-163">**Globally**: To restrict access for every Web API controller, add the **AuthorizeAttribute** filter to the global filter list:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample2.cs)]

<span data-ttu-id="bd7d7-164">**Controlador**: Para restringir o acesso a um controlador específico, adicione o filtro como um atributo ao controlador:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-164">**Controller**: To restrict access for a specific controller, add the filter as an attribute to the controller:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample3.cs)]

<span data-ttu-id="bd7d7-165">**Ação**: Para restringir o acesso a ações específicas, adicione o atributo ao método de ação:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-165">**Action**: To restrict access for specific actions, add the attribute to the action method:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample4.cs)]

<span data-ttu-id="bd7d7-166">Alternativamente, você pode restringir o controlador e, em seguida, permitir acesso anônimo a ações específicas, usando o atributo. `[AllowAnonymous]`</span><span class="sxs-lookup"><span data-stu-id="bd7d7-166">Alternatively, you can restrict the controller and then allow anonymous access to specific actions, by using the `[AllowAnonymous]` attribute.</span></span> <span data-ttu-id="bd7d7-167">No exemplo a `Post` seguir, o método `Get` é restrito, mas o método permite acesso anônimo.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-167">In the following example, the `Post` method is restricted, but the `Get` method allows anonymous access.</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample5.cs)]

<span data-ttu-id="bd7d7-168">Nos exemplos anteriores, o filtro permite que qualquer usuário autenticado acesse os métodos restritos; apenas usuários anônimos são mantidos fora. Você também pode limitar o acesso a usuários específicos ou a usuários em funções específicas:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-168">In the previous examples, the filter allows any authenticated user to access the restricted methods; only anonymous users are kept out. You can also limit access to specific users or to users in specific roles:</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample6.cs)]

> [!NOTE]
> <span data-ttu-id="bd7d7-169">O filtro **AuthorizeAttribute** para controladores de API da Web está localizado no **namespace System.Web.Http.**</span><span class="sxs-lookup"><span data-stu-id="bd7d7-169">The **AuthorizeAttribute** filter for Web API controllers is located in the **System.Web.Http** namespace.</span></span> <span data-ttu-id="bd7d7-170">Há um filtro semelhante para controladores MVC no **namespace System.Web.Mvc,** que não é compatível com controladores de API web.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-170">There is a similar filter for MVC controllers in the **System.Web.Mvc** namespace, which is not compatible with Web API controllers.</span></span>

### <a name="custom-authorization-filters"></a><span data-ttu-id="bd7d7-171">Filtros de autorização personalizados</span><span class="sxs-lookup"><span data-stu-id="bd7d7-171">Custom Authorization Filters</span></span>

<span data-ttu-id="bd7d7-172">Para escrever um filtro de autorização personalizado, deriva de um desses tipos:</span><span class="sxs-lookup"><span data-stu-id="bd7d7-172">To write a custom authorization filter, derive from one of these types:</span></span>

- <span data-ttu-id="bd7d7-173">**AutorizaçãoAtributo**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-173">**AuthorizeAttribute**.</span></span> <span data-ttu-id="bd7d7-174">Estender essa classe para executar a lógica de autorização com base nas funções atuais do usuário e do usuário.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-174">Extend this class to perform authorization logic based on the current user and the user's roles.</span></span>
- <span data-ttu-id="bd7d7-175">**AutorizaçãoFilterAttribute**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-175">**AuthorizationFilterAttribute**.</span></span> <span data-ttu-id="bd7d7-176">Amplie essa classe para executar uma lógica de autorização síncrona que não seja necessariamente baseada no usuário ou função atual.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-176">Extend this class to perform synchronous authorization logic that is not necessarily based on the current user or role.</span></span>
- <span data-ttu-id="bd7d7-177">**IAuthorizationFilter**.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-177">**IAuthorizationFilter**.</span></span> <span data-ttu-id="bd7d7-178">Implementar esta interface para executar a lógica de autorização assíncrona; por exemplo, se sua lógica de autorização fizer chamadas assíncronas de I/O ou rede.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-178">Implement this interface to perform asynchronous authorization logic; for example, if your authorization logic makes asynchronous I/O or network calls.</span></span> <span data-ttu-id="bd7d7-179">(Se sua lógica de autorização estiver vinculada à CPU, é mais simples derivar do **AuthorizationFilterAttribute,** porque então você não precisa escrever um método assíncrono.)</span><span class="sxs-lookup"><span data-stu-id="bd7d7-179">(If your authorization logic is CPU-bound, it is simpler to derive from **AuthorizationFilterAttribute**, because then you don't need to write an asynchronous method.)</span></span>

<span data-ttu-id="bd7d7-180">O diagrama a seguir mostra a hierarquia de classe para a classe **AuthorizeAttribute.**</span><span class="sxs-lookup"><span data-stu-id="bd7d7-180">The following diagram shows the class hierarchy for the **AuthorizeAttribute** class.</span></span>

![](authentication-and-authorization-in-aspnet-web-api/_static/image2.png)

### <a name="authorization-inside-a-controller-action"></a><span data-ttu-id="bd7d7-181">Autorização dentro de uma ação do controlador</span><span class="sxs-lookup"><span data-stu-id="bd7d7-181">Authorization Inside a Controller Action</span></span>

<span data-ttu-id="bd7d7-182">Em alguns casos, você pode permitir que um pedido prossiga, mas mude o comportamento com base no principal.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-182">In some cases, you might allow a request to proceed, but change the behavior based on the principal.</span></span> <span data-ttu-id="bd7d7-183">Por exemplo, as informações que você retorna podem mudar dependendo da função do usuário.</span><span class="sxs-lookup"><span data-stu-id="bd7d7-183">For example, the information that you return might change depending on the user's role.</span></span> <span data-ttu-id="bd7d7-184">Dentro de um método controlador, você pode obter o principal atual da propriedade **ApiController.User.**</span><span class="sxs-lookup"><span data-stu-id="bd7d7-184">Within a controller method, you can get the current principal from the **ApiController.User** property.</span></span>

[!code-csharp[Main](authentication-and-authorization-in-aspnet-web-api/samples/sample7.cs)]
