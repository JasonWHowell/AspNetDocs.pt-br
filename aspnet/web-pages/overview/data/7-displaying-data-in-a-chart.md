---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Exibindo dados em um gráfico com páginas da Web ASP.NET (Razor) | Microsoft Docs
author: rick-anderson
description: Este capítulo explica como exibir dados em um gráfico. Nos capítulos anteriores, você aprendeu a exibir dados manualmente e em uma grade. Este capítulo explica...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542879"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="e7df3-105">Exibindo dados em um gráfico com páginas da Web ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="e7df3-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="e7df3-106">pela [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="e7df3-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="e7df3-107">Este artigo explica como usar um gráfico para exibir dados em um `Chart` site ASP.NET Web Pages (Razor) usando o helper.</span><span class="sxs-lookup"><span data-stu-id="e7df3-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="e7df3-108">**O que você vai aprender:**</span><span class="sxs-lookup"><span data-stu-id="e7df3-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="e7df3-109">Como exibir dados em um gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="e7df3-110">Como estilizar gráficos usando temas incorporados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="e7df3-111">Como salvar gráficos e como cachê-los para um melhor desempenho.</span><span class="sxs-lookup"><span data-stu-id="e7df3-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="e7df3-112">Estas são as ASP.NET características de programação introduzidas no artigo:</span><span class="sxs-lookup"><span data-stu-id="e7df3-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="e7df3-113">O `Chart` ajudante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="e7df3-114">As informações deste artigo se aplicam a ASP.NET Páginas da Web 1.0 e páginas da Web 2.</span><span class="sxs-lookup"><span data-stu-id="e7df3-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="e7df3-115">O Ajudante de Gráficos</span><span class="sxs-lookup"><span data-stu-id="e7df3-115">The Chart Helper</span></span>

<span data-ttu-id="e7df3-116">Quando você quiser exibir seus dados em forma `Chart` gráfica, você pode usar o helper.</span><span class="sxs-lookup"><span data-stu-id="e7df3-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="e7df3-117">O `Chart` ajudante pode renderizar uma imagem que exibe dados em uma variedade de tipos de gráficos.</span><span class="sxs-lookup"><span data-stu-id="e7df3-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="e7df3-118">Suporta muitas opções de formatação e rotulagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="e7df3-119">O `Chart` ajudante pode renderizar mais de 30 tipos de gráficos, incluindo todos os tipos de gráficos com os que você pode estar familiarizado com o Microsoft Excel ou outras ferramentas &#8212; gráficos de área, gráficos de barras, gráficos de colunas, gráficos de linha e gráficos de tortas, juntamente com gráficos mais especializados, como gráficos de ações.</span><span class="sxs-lookup"><span data-stu-id="e7df3-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="e7df3-120">**Descrição do gráfico** ![de área: Imagem do tipo gráfico de área](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="e7df3-121">**Descrição do gráfico** ![de barras: Imagem do tipo gráfico de barras](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="e7df3-122">**Descrição do gráfico** ![da coluna: Imagem do tipo gráfico da coluna](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="e7df3-123">**Descrição do gráfico** ![de linha: Imagem do tipo gráfico de linha](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="e7df3-124">**Descrição do gráfico** ![de tortas: Imagem do tipo gráfico de Pie](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="e7df3-125">**Descrição do gráfico** ![de ações: Imagem do tipo gráfico de ações](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="e7df3-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="e7df3-126">Elementos do gráfico</span><span class="sxs-lookup"><span data-stu-id="e7df3-126">Chart Elements</span></span>

<span data-ttu-id="e7df3-127">Gráficos mostram dados e elementos adicionais como lendas, eixos, séries e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="e7df3-128">A imagem a seguir mostra muitos dos elementos `Chart` do gráfico que você pode personalizar quando você usa o ajudante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="e7df3-129">Este artigo mostra como definir alguns (não todos) desses elementos.</span><span class="sxs-lookup"><span data-stu-id="e7df3-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descrição: Imagem mostrando os elementos do gráfico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="e7df3-131">Criando um gráfico a partir de dados</span><span class="sxs-lookup"><span data-stu-id="e7df3-131">Creating a Chart from Data</span></span>

<span data-ttu-id="e7df3-132">Os dados que você exibe em um gráfico podem ser de uma matriz, dos resultados retornados de um banco de dados ou de dados que estão em um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="e7df3-133">Usando uma matriz</span><span class="sxs-lookup"><span data-stu-id="e7df3-133">Using an Array</span></span>

<span data-ttu-id="e7df3-134">Como explicado em [Introdução à programação ASP.NET de páginas da Web usando a sintaxe de navalha,](https://go.microsoft.com/fwlink/?LinkId=202890)uma matriz permite que você armazene uma coleção de itens semelhantes em uma única variável.</span><span class="sxs-lookup"><span data-stu-id="e7df3-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="e7df3-135">Você pode usar matrizes para conter os dados que deseja incluir em seu gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="e7df3-136">Este procedimento mostra como você pode criar um gráfico a partir de dados em arrays, usando o tipo de gráfico padrão.</span><span class="sxs-lookup"><span data-stu-id="e7df3-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="e7df3-137">Ele também mostra como exibir o gráfico dentro da página.</span><span class="sxs-lookup"><span data-stu-id="e7df3-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="e7df3-138">Crie um novo arquivo chamado *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="e7df3-139">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="e7df3-140">O código primeiro cria um novo gráfico e define sua largura e altura.</span><span class="sxs-lookup"><span data-stu-id="e7df3-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="e7df3-141">Você especifica o título `AddTitle` do gráfico usando o método.</span><span class="sxs-lookup"><span data-stu-id="e7df3-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="e7df3-142">Para adicionar dados, `AddSeries` você usa o método.</span><span class="sxs-lookup"><span data-stu-id="e7df3-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="e7df3-143">Neste exemplo, você `name`usa `xValue`os `yValues` parâmetros `AddSeries` e parâmetros do método.</span><span class="sxs-lookup"><span data-stu-id="e7df3-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="e7df3-144">O `name` parâmetro é exibido na legenda do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="e7df3-145">O `xValue` parâmetro contém um conjunto de dados exibidos ao longo do eixo horizontal do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="e7df3-146">O `yValues` parâmetro contém um conjunto de dados que é usado para traçar os pontos verticais do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="e7df3-147">O `Write` método realmente renderiza o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="e7df3-148">Neste caso, como você não especificou `Chart` um tipo de gráfico, o ajudante renderiza seu gráfico padrão, que é um gráfico de colunas.</span><span class="sxs-lookup"><span data-stu-id="e7df3-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="e7df3-149">Execute a página no navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-149">Run the page in the browser.</span></span> <span data-ttu-id="e7df3-150">O navegador exibe o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="e7df3-151">Usando uma consulta de banco de dados para dados gráficos</span><span class="sxs-lookup"><span data-stu-id="e7df3-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="e7df3-152">Se as informações que você deseja mapear estão em um banco de dados, você pode executar uma consulta de banco de dados e, em seguida, usar dados dos resultados para criar o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="e7df3-153">Este procedimento mostra como ler e exibir os dados do banco de dados criado no artigo [Introdução ao Trabalho com um Banco de Dados em sites de páginas ASP.NET.](https://go.microsoft.com/fwlink/?LinkId=202893)</span><span class="sxs-lookup"><span data-stu-id="e7df3-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="e7df3-154">Adicione uma pasta *De dados\_* do aplicativo à raiz do site se a pasta ainda não existir.</span><span class="sxs-lookup"><span data-stu-id="e7df3-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="e7df3-155">Na pasta Dados do *\_aplicativo,* adicione o arquivo de banco de dados chamado *SmallBakery.sdf* descrito em [Introdução ao Trabalho com um Banco de Dados em sites de páginas da ASP.NET.](https://go.microsoft.com/fwlink/?LinkId=202893)</span><span class="sxs-lookup"><span data-stu-id="e7df3-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="e7df3-156">Crie um novo arquivo chamado *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="e7df3-157">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="e7df3-158">O código primeiro abre o banco de dados `db`SmallBakery e atribui-o a uma variável chamada .</span><span class="sxs-lookup"><span data-stu-id="e7df3-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="e7df3-159">Esta variável representa `Database` um objeto que pode ser usado para ler e gravar no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="e7df3-160">Em seguida, o código executa uma consulta SQL para obter o nome e o preço de cada produto.</span><span class="sxs-lookup"><span data-stu-id="e7df3-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="e7df3-161">O código cria um novo gráfico e passa a consulta `DataBindTable` do banco de dados para ele chamando o método do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="e7df3-162">Este método tem dois `dataSource` parâmetros: o parâmetro é para `xField` os dados da consulta, e o parâmetro permite definir qual coluna de dados é usada para o eixo x do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="e7df3-163">Como alternativa ao `DataBindTable` uso do método, `AddSeries` você `Chart` pode usar o método do ajudante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="e7df3-164">O `AddSeries` método permite definir `xValue` `yValues` os parâmetros e.</span><span class="sxs-lookup"><span data-stu-id="e7df3-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="e7df3-165">Por exemplo, em `DataBindTable` vez de usar o método como este:</span><span class="sxs-lookup"><span data-stu-id="e7df3-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="e7df3-166">Você pode `AddSeries` usar o método assim:</span><span class="sxs-lookup"><span data-stu-id="e7df3-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="e7df3-167">Ambos premem os mesmos resultados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-167">Both render the same results.</span></span> <span data-ttu-id="e7df3-168">O `AddSeries` método é mais flexível porque você pode especificar o `DataBindTable` tipo de gráfico e os dados de forma mais explícita, mas o método é mais fácil de usar se você não precisar da flexibilidade extra.</span><span class="sxs-lookup"><span data-stu-id="e7df3-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="e7df3-169">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="e7df3-170">Usando dados XML</span><span class="sxs-lookup"><span data-stu-id="e7df3-170">Using XML Data</span></span>

<span data-ttu-id="e7df3-171">A terceira opção para gráficos é usar um arquivo XML como os dados para o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="e7df3-172">Isso requer que o arquivo XML também tenha um arquivo de esquema (arquivo *.xsd)* que descreve a estrutura XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="e7df3-173">Este procedimento mostra como ler dados de um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="e7df3-174">Na pasta Dados do *aplicativo,\_* crie um novo arquivo XML chamado *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="e7df3-175">Substitua o XML existente pelo seguinte, que são alguns dados XML sobre funcionários em uma empresa fictícia.</span><span class="sxs-lookup"><span data-stu-id="e7df3-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="e7df3-176">Na pasta Dados do *aplicativo,\_* crie um novo arquivo XML chamado *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="e7df3-177">(Observe que a extensão desta vez é *.xsd*.)</span><span class="sxs-lookup"><span data-stu-id="e7df3-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="e7df3-178">Substitua o XML existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="e7df3-179">Na raiz do site, crie um novo arquivo chamado *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="e7df3-180">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="e7df3-181">O código primeiro `DataSet` cria um objeto.</span><span class="sxs-lookup"><span data-stu-id="e7df3-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="e7df3-182">Este objeto é usado para gerenciar os dados lidos do arquivo XML e organizá-los de acordo com as informações no arquivo do esquema.</span><span class="sxs-lookup"><span data-stu-id="e7df3-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="e7df3-183">(Observe que a parte superior do `using SystemData`código inclui a declaração .</span><span class="sxs-lookup"><span data-stu-id="e7df3-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="e7df3-184">Isso é necessário para poder trabalhar `DataSet` com o objeto.</span><span class="sxs-lookup"><span data-stu-id="e7df3-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="e7df3-185">Para obter mais informações, consulte [ &quot;Usando&quot; declarações e nomes totalmente qualificados](#SB_UsingStatements) mais tarde neste artigo.)</span><span class="sxs-lookup"><span data-stu-id="e7df3-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="e7df3-186">Em seguida, o `DataView` código cria um objeto com base no conjunto de dados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="e7df3-187">A exibição de dados fornece um objeto que o gráfico pode vincular a &#8212; que seja, ler e plotar.</span><span class="sxs-lookup"><span data-stu-id="e7df3-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="e7df3-188">O gráfico se liga aos `AddSeries` dados usando o método, como você viu anteriormente `xValue` `yValues` ao mapear os `DataView` dados do array, exceto que desta vez os parâmetros e parâmetros são definidos para o objeto.</span><span class="sxs-lookup"><span data-stu-id="e7df3-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="e7df3-189">Este exemplo também mostra como especificar um determinado tipo de gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="e7df3-190">Quando os dados são `AddSeries` adicionados `chartType` no método, o parâmetro também é definido para exibir um gráfico de tortas.</span><span class="sxs-lookup"><span data-stu-id="e7df3-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="e7df3-191">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="e7df3-192">"Usando" declarações e nomes totalmente qualificados</span><span class="sxs-lookup"><span data-stu-id="e7df3-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="e7df3-193">O Framework .NET que ASP.NET Páginas da Web com sintaxe de navalha é baseado em muitos milhares de componentes (classes).</span><span class="sxs-lookup"><span data-stu-id="e7df3-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="e7df3-194">Para torná-lo gerenciável para trabalhar com todas essas classes, eles são organizados em *namespaces*, que são um pouco como bibliotecas.</span><span class="sxs-lookup"><span data-stu-id="e7df3-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="e7df3-195">Por exemplo, `System.Web` o namespace contém classes que `System.Xml` suportam a comunicação do navegador/servidor, o namespace contém classes usadas para criar e ler arquivos XML e o `System.Data` namespace contém classes que permitem trabalhar com dados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="e7df3-196">Para acessar qualquer classe dada no .NET Framework, o código precisa saber não apenas o nome da classe, mas também o namespace em que a classe está.</span><span class="sxs-lookup"><span data-stu-id="e7df3-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="e7df3-197">Por exemplo, para usar `Chart` o ajudante, o `System.Web.Helpers.Chart` código precisa encontrar a classe, que combina o namespace (`System.Web.Helpers`) com o nome da classe (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="e7df3-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="e7df3-198">Isso é conhecido como o nome *totalmente qualificado* da classe &#8212; sua localização completa e inequívoca dentro da vastidão do Quadro .NET.</span><span class="sxs-lookup"><span data-stu-id="e7df3-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="e7df3-199">Em código, isso se pareceria com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="e7df3-200">No entanto, é complicado (e propenso a erros) ter que usar esses nomes longos e totalmente qualificados toda vez que você quiser se referir a uma classe ou ajudante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="e7df3-201">Portanto, para facilitar o uso de nomes de classe, você pode *importar* os namespaces que você está interessado, o que geralmente é apenas um punhado entre os muitos namespaces no .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e7df3-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="e7df3-202">Se você importou um namespace, você pode usar`Chart`apenas um nome de`System.Web.Helpers.Chart`classe ( ) em vez do nome totalmente qualificado ().</span><span class="sxs-lookup"><span data-stu-id="e7df3-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="e7df3-203">Quando seu código é executado e encontra um nome de classe, ele pode olhar apenas nos namespaces que você importou para encontrar essa classe.</span><span class="sxs-lookup"><span data-stu-id="e7df3-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="e7df3-204">Quando você usa ASP.NET Páginas da Web com sintaxe de Navalha para criar `WebPage` páginas da Web, você normalmente usa o mesmo conjunto de classes todas as vezes, incluindo a classe, os vários ajudantes e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="e7df3-205">Para salvar o trabalho de importar os namespaces relevantes toda vez que você criar um site, ASP.NET é configurado para que ele importe automaticamente um conjunto de espaços de nomes principais para cada site.</span><span class="sxs-lookup"><span data-stu-id="e7df3-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="e7df3-206">É por isso que você não teve que lidar com namespaces ou importação até agora; todas as aulas com as quais você trabalhou estão em espaços de nomes que já são importados para você.</span><span class="sxs-lookup"><span data-stu-id="e7df3-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="e7df3-207">No entanto, às vezes você precisa trabalhar com uma classe que não está em um namespace que é automaticamente importado para você.</span><span class="sxs-lookup"><span data-stu-id="e7df3-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="e7df3-208">Nesse caso, você pode usar o nome totalmente qualificado dessa classe ou importar manualmente o namespace que contém a classe.</span><span class="sxs-lookup"><span data-stu-id="e7df3-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="e7df3-209">Para importar um namespace, `using` você`import` usa a declaração (no Visual Basic), como você viu em um exemplo anterior do artigo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="e7df3-210">Por exemplo, `DataSet` a classe `System.Data` está no namespace.</span><span class="sxs-lookup"><span data-stu-id="e7df3-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="e7df3-211">O `System.Data` namespace não está disponível automaticamente para ASP.NET páginas de Navalha.</span><span class="sxs-lookup"><span data-stu-id="e7df3-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="e7df3-212">Portanto, para trabalhar `DataSet` com a classe usando seu nome totalmente qualificado, você pode usar código como este:</span><span class="sxs-lookup"><span data-stu-id="e7df3-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="e7df3-213">Se você tiver `DataSet` que usar a classe repetidamente, você pode importar um namespace como este e, em seguida, usar apenas o nome da classe em código:</span><span class="sxs-lookup"><span data-stu-id="e7df3-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="e7df3-214">Você pode `using` adicionar instruções para quaisquer outros namespaces de nome .NET Framework que você deseja referenciar.</span><span class="sxs-lookup"><span data-stu-id="e7df3-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="e7df3-215">No entanto, como observado, você não precisará fazer isso com frequência, porque a maioria das classes com as quais você trabalhará estão em espaços de nome que são importados automaticamente por ASP.NET para uso em páginas *.cshtml* e *.vbhtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="e7df3-216">Exibindo gráficos dentro de uma página da Web</span><span class="sxs-lookup"><span data-stu-id="e7df3-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="e7df3-217">Nos exemplos que você viu até agora, você cria um gráfico e, em seguida, o gráfico é renderizado diretamente para o navegador como um gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="e7df3-218">Em muitos casos, porém, você deseja exibir um gráfico como parte de uma página, não apenas por si só no navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="e7df3-219">Para fazer isso é necessário um processo de duas etapas.</span><span class="sxs-lookup"><span data-stu-id="e7df3-219">To do that requires a two-step process.</span></span> <span data-ttu-id="e7df3-220">O primeiro passo é criar uma página que gere o gráfico, como você já viu.</span><span class="sxs-lookup"><span data-stu-id="e7df3-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="e7df3-221">O segundo passo é exibir a imagem resultante em outra página.</span><span class="sxs-lookup"><span data-stu-id="e7df3-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="e7df3-222">Para exibir a imagem, `<img>` você usa um elemento HTML, da mesma forma que você faria para exibir qualquer imagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="e7df3-223">No entanto, em vez de fazer referência a um arquivo *.jpg* ou `Chart` *.png,* o `<img>` elemento faz referência ao arquivo *.cshtml* que contém o helper que cria o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="e7df3-224">Quando a página de `<img>` exibição é `Chart` executada, o elemento obtém a saída do ajudante e renderiza o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="e7df3-225">Crie um arquivo chamado *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="e7df3-226">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="e7df3-227">O código `<img>` usa o elemento para exibir o gráfico que você criou anteriormente no arquivo *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="e7df3-228">Execute a página da Web em um navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-228">Run the web page in a browser.</span></span> <span data-ttu-id="e7df3-229">O arquivo *ShowChart.cshtml* exibe a imagem do gráfico com base no código contido no arquivo *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="e7df3-230">Estilizando um Gráfico</span><span class="sxs-lookup"><span data-stu-id="e7df3-230">Styling a Chart</span></span>

<span data-ttu-id="e7df3-231">O `Chart` ajudante suporta um grande número de opções que permitem personalizar a aparência do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="e7df3-232">Você pode definir cores, fontes, bordas e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="e7df3-233">Uma maneira fácil de personalizar a aparência de um gráfico é usar um *tema*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="e7df3-234">Os temas são conjuntos de informações que especificam como renderizar um gráfico usando fontes, cores, rótulos, paletas, bordas e efeitos.</span><span class="sxs-lookup"><span data-stu-id="e7df3-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="e7df3-235">(Observe que o estilo de um gráfico não indica o tipo de gráfico.)</span><span class="sxs-lookup"><span data-stu-id="e7df3-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="e7df3-236">A tabela a seguir lista temas incorporados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="e7df3-237">Tema</span><span class="sxs-lookup"><span data-stu-id="e7df3-237">Theme</span></span> | <span data-ttu-id="e7df3-238">Descrição</span><span class="sxs-lookup"><span data-stu-id="e7df3-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="e7df3-239">Exibe colunas vermelhas em um fundo branco.</span><span class="sxs-lookup"><span data-stu-id="e7df3-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="e7df3-240">Exibe colunas azuis em um fundo gradiente azul.</span><span class="sxs-lookup"><span data-stu-id="e7df3-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="e7df3-241">Exibe colunas azuis em um fundo gradiente verde.</span><span class="sxs-lookup"><span data-stu-id="e7df3-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="e7df3-242">Exibe colunas laranjas em um fundo gradiente amarelo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="e7df3-243">Exibe colunas vermelhas 3D em um fundo branco.</span><span class="sxs-lookup"><span data-stu-id="e7df3-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="e7df3-244">Você pode especificar o tema a ser usado quando criar um novo gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="e7df3-245">Crie um novo arquivo chamado *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="e7df3-246">Substitua o conteúdo existente na página pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="e7df3-247">Este código é o mesmo que o exemplo anterior que `theme` usa o banco `Chart` de dados para dados, mas adiciona o parâmetro quando cria o objeto.</span><span class="sxs-lookup"><span data-stu-id="e7df3-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="e7df3-248">A seguir, mostra o código alterado:</span><span class="sxs-lookup"><span data-stu-id="e7df3-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="e7df3-249">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-249">Run the page in a browser.</span></span> <span data-ttu-id="e7df3-250">Você vê os mesmos dados de antes, mas o gráfico parece mais polido:</span><span class="sxs-lookup"><span data-stu-id="e7df3-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="e7df3-251">Salvando um gráfico</span><span class="sxs-lookup"><span data-stu-id="e7df3-251">Saving a Chart</span></span>

<span data-ttu-id="e7df3-252">Quando você `Chart` usa o ajudante como você viu até agora neste artigo, o ajudante recria o gráfico do zero cada vez que é invocado.</span><span class="sxs-lookup"><span data-stu-id="e7df3-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="e7df3-253">Se necessário, o código do gráfico também reconsulta o banco de dados ou relê o arquivo XML para obter os dados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="e7df3-254">Em alguns casos, fazer isso pode ser uma operação complexa, como se o banco de dados que você está consultando é grande, ou se o arquivo XML contém um monte de dados.</span><span class="sxs-lookup"><span data-stu-id="e7df3-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="e7df3-255">Mesmo que o gráfico não envolva muitos dados, o processo de criação dinâmica de uma imagem ocupa recursos do servidor, e se muitas pessoas solicitarem a página ou páginas que exibem o gráfico, pode haver um impacto no desempenho do seu site.</span><span class="sxs-lookup"><span data-stu-id="e7df3-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="e7df3-256">Para ajudá-lo a reduzir o impacto potencial de desempenho da criação de um gráfico, você pode criar um gráfico na primeira vez que precisar dele e, em seguida, salvá-lo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="e7df3-257">Quando o gráfico é necessário novamente, em vez de recomodá-lo, você pode simplesmente buscar a versão salva e render isso.</span><span class="sxs-lookup"><span data-stu-id="e7df3-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="e7df3-258">Você pode salvar um gráfico desta maneira:</span><span class="sxs-lookup"><span data-stu-id="e7df3-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="e7df3-259">Cache do gráfico na memória do computador (no servidor).</span><span class="sxs-lookup"><span data-stu-id="e7df3-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="e7df3-260">Salve o gráfico como um arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="e7df3-261">Salve o gráfico como um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-261">Save the chart as an XML file.</span></span> <span data-ttu-id="e7df3-262">Esta opção permite modificar o gráfico antes de salvá-lo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="e7df3-263">Cache andaum gráfico</span><span class="sxs-lookup"><span data-stu-id="e7df3-263">Caching a Chart</span></span>

<span data-ttu-id="e7df3-264">Depois de criar um gráfico, você pode cachê-lo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="e7df3-265">Cache arqueiar um gráfico significa que ele não precisa ser recriado se precisar ser exibido novamente.</span><span class="sxs-lookup"><span data-stu-id="e7df3-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="e7df3-266">Quando você salva um gráfico no cache, você dá a ele uma chave que deve ser exclusiva desse gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="e7df3-267">Os gráficos salvos no cache podem ser removidos se o servidor ficar sem memória.</span><span class="sxs-lookup"><span data-stu-id="e7df3-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="e7df3-268">Além disso, o cache é limpo se o aplicativo for reiniciado por qualquer motivo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="e7df3-269">Portanto, a maneira padrão de trabalhar com um gráfico em cache é sempre verificar primeiro se ele está disponível no cache e, se não, para criá-lo ou recriá-lo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="e7df3-270">Na raiz do seu site, crie um arquivo chamado *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="e7df3-271">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="e7df3-272">A `<img>` tag inclui `src` um atributo que aponta para o arquivo *ChartSaveToCache.cshtml* e passa uma chave para a página como uma seqüência de consulta.</span><span class="sxs-lookup"><span data-stu-id="e7df3-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="e7df3-273">A chave contém &quot;o&quot;valor myChartKey .</span><span class="sxs-lookup"><span data-stu-id="e7df3-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="e7df3-274">O arquivo *ChartSaveToCache.cshtml* contém o `Chart` helper que cria o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="e7df3-275">Você criará esta página em um momento.</span><span class="sxs-lookup"><span data-stu-id="e7df3-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="e7df3-276">No final da página há um link para uma página chamada *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="e7df3-277">Essa é uma página que você também criará em breve.</span><span class="sxs-lookup"><span data-stu-id="e7df3-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="e7df3-278">Você precisa do *ClearCache.cshtml* apenas para testar o cache para este exemplo — não é um link ou página que você normalmente incluiria ao trabalhar com gráficos em cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="e7df3-279">Na raiz do seu site, crie um novo arquivo chamado *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="e7df3-280">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="e7df3-281">O código primeiro verifica se algo foi passado como o valor-chave na seqüência de consultas.</span><span class="sxs-lookup"><span data-stu-id="e7df3-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="e7df3-282">Se assim for, o código tenta ler um gráfico `GetFromCache` para fora do cache, chamando o método e passando-o a chave.</span><span class="sxs-lookup"><span data-stu-id="e7df3-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="e7df3-283">Se não houver nada no cache sob essa tecla (o que aconteceria na primeira vez que o gráfico é solicitado), o código cria o gráfico como de costume.</span><span class="sxs-lookup"><span data-stu-id="e7df3-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="e7df3-284">Quando o gráfico é concluído, o código o `SaveToCache`salva no cache chamando .</span><span class="sxs-lookup"><span data-stu-id="e7df3-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="e7df3-285">Esse método requer uma chave (para que o gráfico possa ser solicitado mais tarde) e a quantidade de tempo que o gráfico deve ser salvo no cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="e7df3-286">(A hora exata em que você armazena um gráfico dependeria de quantas vezes você pensou que os dados que ele representa poderiam mudar.) O `SaveToCache` método também `slidingExpiration` requer um parâmetro &#8212; se isso for definido como verdadeiro, o contador de tempo é redefinido cada vez que o gráfico é acessado.</span><span class="sxs-lookup"><span data-stu-id="e7df3-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="e7df3-287">Neste caso, isso significa que a entrada de cache do gráfico expira 2 minutos após a última vez que alguém acessou o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="e7df3-288">(A alternativa ao vencimento deslizante é a expiração absoluta, o que significa que a entrada do cache expiraria exatamente 2 minutos após ser colocada no cache, não importa quantas vezes tivesse sido acessada.)</span><span class="sxs-lookup"><span data-stu-id="e7df3-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="e7df3-289">Finalmente, o código `WriteFromCache` usa o método para buscar e renderizar o gráfico a partir do cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="e7df3-290">Observe que este método `if` está fora do bloco que verifica o cache, porque ele obterá o gráfico do cache se o gráfico estava lá para começar ou teve que ser gerado e salvo no cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="e7df3-291">Observe que, no `AddTitle` exemplo, o método inclui um carimbo de data e hora.</span><span class="sxs-lookup"><span data-stu-id="e7df3-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="e7df3-292">(Ele adiciona a data `DateTime.Now` e a hora atuais &#8212; &#8212; ao título.)</span><span class="sxs-lookup"><span data-stu-id="e7df3-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="e7df3-293">Crie uma nova página chamada *ClearCache.cshtml* e substitua seu conteúdo pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="e7df3-294">Esta página `WebCache` usa o helper para remover o gráfico armazenado em cache no *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="e7df3-295">Como observado anteriormente, você normalmente não precisa ter uma página como esta.</span><span class="sxs-lookup"><span data-stu-id="e7df3-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="e7df3-296">Você está criando aqui apenas para facilitar o teste de cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="e7df3-297">Execute a página *showCachedChart.cshtml* em um navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="e7df3-298">A página exibe a imagem do gráfico com base no código contido no arquivo *ChartSaveToCache.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="e7df3-299">Anote o que diz o carimbo de data e hora no título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descrição: Imagem do gráfico básico com carimbo de tempo no título do gráfico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="e7df3-301">Feche o navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-301">Close the browser.</span></span>
8. <span data-ttu-id="e7df3-302">Execute o *ShowCachedChart.cshtml* novamente.</span><span class="sxs-lookup"><span data-stu-id="e7df3-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="e7df3-303">Observe que o carimbo de tempo é o mesmo de antes, o que indica que o gráfico não foi regenerado, mas foi lido a partir do cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="e7df3-304">Em *ShowCachedChart.cshtml,* clique no link **Limpar cache.**</span><span class="sxs-lookup"><span data-stu-id="e7df3-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="e7df3-305">Isso leva você ao *ClearCache.cshtml*, que informa que o cache foi limpo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="e7df3-306">Clique no **link 'Retornar ao ShowCachedChart.cshtml'** ou reexecute *ShowCachedChart.cshtml* do WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="e7df3-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="e7df3-307">Observe que desta vez o carimbo de tempo foi alterado, porque o cache foi limpo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="e7df3-308">Portanto, o código teve que regenerar o gráfico e colocá-lo de volta no cache.</span><span class="sxs-lookup"><span data-stu-id="e7df3-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="e7df3-309">Salvando um gráfico como um arquivo de imagem</span><span class="sxs-lookup"><span data-stu-id="e7df3-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="e7df3-310">Você também pode salvar um gráfico como um arquivo de imagem (por exemplo, como um arquivo *.jpg)* no servidor.</span><span class="sxs-lookup"><span data-stu-id="e7df3-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="e7df3-311">Em seguida, você pode usar o arquivo de imagem da maneira que você faria qualquer imagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="e7df3-312">A vantagem é que o arquivo é armazenado em vez de salvo em um cache temporário.</span><span class="sxs-lookup"><span data-stu-id="e7df3-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="e7df3-313">Você pode salvar uma nova imagem de gráfico em momentos diferentes (por exemplo, a cada hora) e, em seguida, manter um registro permanente das alterações que ocorrem ao longo do tempo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="e7df3-314">Observe que você deve ter certeza de que seu aplicativo web tem permissão para salvar um arquivo para a pasta no servidor onde você deseja colocar o arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="e7df3-315">Na raiz do seu site, crie uma pasta chamada \* \_ChartFiles\* se ela ainda não existir.</span><span class="sxs-lookup"><span data-stu-id="e7df3-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="e7df3-316">Na raiz do seu site, crie um novo arquivo chamado *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="e7df3-317">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="e7df3-318">O código primeiro verifica se o arquivo *.jpg* existe chamando o `File.Exists` método.</span><span class="sxs-lookup"><span data-stu-id="e7df3-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="e7df3-319">Se o arquivo não existir, o `Chart` código cria um novo a partir de uma matriz.</span><span class="sxs-lookup"><span data-stu-id="e7df3-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="e7df3-320">Desta vez, o `Save` código chama `path` o método e passa o parâmetro para especificar o caminho do arquivo e o nome do arquivo de onde salvar o gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="e7df3-321">No corpo da página, `<img>` um elemento usa o caminho para apontar para o arquivo *.jpg* para exibir.</span><span class="sxs-lookup"><span data-stu-id="e7df3-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="e7df3-322">Execute o arquivo *ChartSave.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="e7df3-323">Retorne ao WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="e7df3-323">Return to WebMatrix.</span></span> <span data-ttu-id="e7df3-324">Observe que um arquivo de imagem chamado *chart01.jpg* foi salvo na \* \_pasta ChartFiles.\*</span><span class="sxs-lookup"><span data-stu-id="e7df3-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="e7df3-325">Salvando um gráfico como um arquivo XML</span><span class="sxs-lookup"><span data-stu-id="e7df3-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="e7df3-326">Finalmente, você pode salvar um gráfico como um arquivo XML no servidor.</span><span class="sxs-lookup"><span data-stu-id="e7df3-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="e7df3-327">Uma vantagem de usar este método sobre cachedo do gráfico ou salvar o gráfico em um arquivo é que você pode modificar o XML antes de exibir o gráfico se você quiser.</span><span class="sxs-lookup"><span data-stu-id="e7df3-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="e7df3-328">Seu aplicativo tem que ter permissões de leitura/gravação para a pasta no servidor onde você deseja colocar o arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="e7df3-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="e7df3-329">Na raiz do seu site, crie um novo arquivo chamado *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="e7df3-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="e7df3-330">Substitua o conteúdo existente pelo seguinte:</span><span class="sxs-lookup"><span data-stu-id="e7df3-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="e7df3-331">Este código é semelhante ao código que você viu anteriormente para armazenar um gráfico no cache, exceto que ele usa um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="e7df3-332">O código primeiro verifica se o arquivo XML existe chamando o `File.Exists` método.</span><span class="sxs-lookup"><span data-stu-id="e7df3-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="e7df3-333">Se o arquivo existir, o `Chart` código cria um novo `themePath` objeto e passa o nome do arquivo como parâmetro.</span><span class="sxs-lookup"><span data-stu-id="e7df3-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="e7df3-334">Isso cria o gráfico com base no que está no arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="e7df3-335">Se o arquivo XML ainda não existir, o código cria `SaveXml` um gráfico como o normal e, em seguida, chama para salvá-lo.</span><span class="sxs-lookup"><span data-stu-id="e7df3-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="e7df3-336">O gráfico é renderizado usando o `Write` método, como você já viu antes.</span><span class="sxs-lookup"><span data-stu-id="e7df3-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="e7df3-337">Assim como na página que mostrava cache, este código inclui um carimbo de data e hora no título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="e7df3-338">Crie uma nova página chamada *ChartDisplayXMLChart.cshtml* e adicione a seguinte marcação:</span><span class="sxs-lookup"><span data-stu-id="e7df3-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="e7df3-339">Execute a página *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="e7df3-340">O gráfico é exibido.</span><span class="sxs-lookup"><span data-stu-id="e7df3-340">The chart is displayed.</span></span> <span data-ttu-id="e7df3-341">Anote o carimbo de tempo no título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="e7df3-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="e7df3-342">Feche o navegador.</span><span class="sxs-lookup"><span data-stu-id="e7df3-342">Close the browser.</span></span>
6. <span data-ttu-id="e7df3-343">No WebMatrix, clique com o botão **Refresh**direito do mouse na \* \_pasta ChartFiles,\* clique em Atualizar e, em seguida, abra a pasta.</span><span class="sxs-lookup"><span data-stu-id="e7df3-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="e7df3-344">O arquivo *XMLChart.xml* nesta pasta foi `Chart` criado pelo ajudante.</span><span class="sxs-lookup"><span data-stu-id="e7df3-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descrição: A pasta _ChartFiles mostrando o arquivo XMLChart.xml criado pelo ajudante de gráfico.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="e7df3-346">Execute novamente a página *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="e7df3-347">O gráfico mostra o mesmo carimbo de tempo da primeira vez que você executou a página.</span><span class="sxs-lookup"><span data-stu-id="e7df3-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="e7df3-348">Isso porque o gráfico está sendo gerado a partir do XML que você salvou anteriormente.</span><span class="sxs-lookup"><span data-stu-id="e7df3-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="e7df3-349">No WebMatrix, \* \_\* abra a pasta ChartFiles e exclua o arquivo *XMLChart.xml.*</span><span class="sxs-lookup"><span data-stu-id="e7df3-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="e7df3-350">Execute a página *ChartDisplayXMLChart.cshtml* mais uma vez.</span><span class="sxs-lookup"><span data-stu-id="e7df3-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="e7df3-351">Desta vez, o carimbo de `Chart` tempo é atualizado, porque o ajudante teve que recriar o arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="e7df3-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="e7df3-352">Se você quiser, \* \_\* verifique a pasta ChartFiles e observe que o arquivo XML está de volta.</span><span class="sxs-lookup"><span data-stu-id="e7df3-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="e7df3-353">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="e7df3-353">Additional Resources</span></span>

- [<span data-ttu-id="e7df3-354">Introdução ao trabalho com um banco de dados em sites de páginas da Web ASP.NET</span><span class="sxs-lookup"><span data-stu-id="e7df3-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="e7df3-355">Usando cache em sites de páginas da Web ASP.NET para melhorar o desempenho</span><span class="sxs-lookup"><span data-stu-id="e7df3-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="e7df3-356">[Classe de gráficos](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET referência de API de páginas da Web no MSDN)</span><span class="sxs-lookup"><span data-stu-id="e7df3-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
